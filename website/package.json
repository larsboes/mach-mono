{
  "name": "website",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "prebuild": "node -e \"const fs = require('node:fs'); const path = 'public/resources/images'; try { const stat = fs.statSync(path); if (!stat.isDirectory()) { throw new Error('not a directory'); } } catch (error) { console.error('Missing or invalid website resources path: ' + path); process.exit(1); }\"",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.41.5",
    "astro": "^7.1.4",
    "astro-mermaid": "^2.0.1",
    "mermaid": "^11.15.0"
  },
  "overrides": {
    "devalue": "5.8.1"
  }
}
