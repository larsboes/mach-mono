name: CI

on:
  push:
    branches:
      - '*'
  pull_request:
    branches:
      - '*'

# https://stackoverflow.com/a/72408109/6942800
concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

# Least-privilege: only needs to read code for building.
permissions:
  contents: read

jobs:
  build:
    name: Build
    runs-on: macos-latest
    steps:
    - name: Code Checkout
      uses: actions/checkout@v4
    - name: Build machNotch
      run: |
        xcodebuild -project Apps/machNotch/machNotch.xcodeproj \
          -scheme machNotch \
          -destination 'platform=macOS' \
          -configuration Release \
          build

  test:
    name: Test
    runs-on: macos-latest
    steps:
    - uses: actions/checkout@v4
    - name: Test machNotch
      run: |
        xcodebuild -project Apps/machNotch/machNotch.xcodeproj \
          -scheme machNotch \
          -destination 'platform=macOS' \
          test

  arch-check:
    name: Architecture Check
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Run architecture checks
      run: bash .github/scripts/arch-check.sh
