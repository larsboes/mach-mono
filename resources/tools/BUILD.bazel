# Bazel package marker so other BUILD files can `load("//resources/tools:defs.bzl", ...)`.
# No targets are defined here — this directory only exposes shared `.bzl` constants.

exports_files(
    ["defs.bzl"],
    visibility = ["//visibility:public"],
)
